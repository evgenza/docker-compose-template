# Docker Compose Template

Шаблон для быстрого запуска инфраструктурных сервисов с помощью единого Docker Compose файла. Включает готовые конфигурации для баз данных, брокеров сообщений, мониторинга и хранилищ объектов.

## 🎯 Основные преимущества

- ✅ **Единый `docker-compose.yaml`** — все сервисы в одном файле
- ✅ **Профили для выбора сервисов** — запускайте только то, что нужно
- ✅ **Единый `.env` файл** — все секреты и настройки в одном месте
- ✅ **Автоматическая генерация секретов** — скрипт создает безопасные пароли
- ✅ **Общая сеть** — все сервисы видят друг друга
- ✅ **Минимальная настройка** — запустил скрипт, выбрал профили, запустил

## 📁 Структура проекта

```
docker-compose-template/
├── docker-compose.yaml          # Единый файл со всеми сервисами
├── .env.example                 # Пример переменных окружения
├── generate-secrets.sh          # Общий скрипт для генерации всех секретов
├── postgres/                    # Скрипты инициализации PostgreSQL
│   └── generate-secrets.sh      # Скрипт генерации секретов для PostgreSQL
├── mongodb/                     # Скрипты инициализации MongoDB
│   └── generate-secrets.sh      # Скрипт генерации секретов для MongoDB
├── redis/                       # Redis
│   └── generate-secrets.sh      # Скрипт генерации секретов для Redis
├── kafka/                       # Kafka
│   └── generate-secrets.sh      # Информационный скрипт (Kafka без паролей)
├── rabbitmq/                    # RabbitMQ
│   └── generate-secrets.sh      # Скрипт генерации секретов для RabbitMQ
├── minio/                       # MinIO
│   └── generate-secrets.sh      # Скрипт генерации секретов для MinIO
├── prometheus-grafana/          # Конфигурация Prometheus и Grafana
│   └── generate-secrets.sh      # Скрипт генерации секретов для Grafana
└── README.md
```

## 🚀 Быстрый старт

### Шаг 1: Настройка переменных окружения

```bash
# Автоматическая генерация секретов (рекомендуется)
./generate-secrets.sh

# ИЛИ ручная настройка
cp .env.example .env
# Отредактируйте .env и установите безопасные пароли
```

### Шаг 2: Выбор и запуск нужных сервисов

Используйте профили для выбора сервисов:

```bash
# Запуск только PostgreSQL
docker-compose --profile postgres up -d

# Запуск нескольких сервисов
docker-compose --profile postgres --profile redis --profile kafka up -d

# Запуск всех сервисов
docker-compose --profile postgres --profile mongodb --profile redis \
               --profile kafka --profile rabbitmq --profile minio \
               --profile monitoring up -d
```

### Шаг 3: Проверка статуса

```bash
docker-compose ps
```

## 🎬 Использование шаблона для нового проекта

Для создания нового проекта из этого шаблона:

1. **Скопируйте шаблон** в ваш проект:
   ```bash
   cp docker-compose.yaml /path/to/your/project/
   cp .env.example /path/to/your/project/
   cp generate-secrets.sh /path/to/your/project/
   chmod +x /path/to/your/project/generate-secrets.sh
   ```

2. **Сгенерируйте секреты** (или настройте вручную):
   ```bash
   cd /path/to/your/project/
   ./generate-secrets.sh
   ```

3. **Выберите нужные сервисы** — используйте профили при запуске:
   ```bash
   docker-compose --profile postgres --profile redis up -d
   ```

4. **Готово!** Теперь у вас есть единый `docker-compose.yaml` с нужными сервисами

## 📦 Доступные профили сервисов

### Базы данных

| Профиль | Сервисы | Порты | Описание |
|---------|---------|-------|----------|
| `postgres` | PostgreSQL + pgAdmin | 5432 / 5050 | PostgreSQL 15.8 с веб-интерфейсом |
| `mongodb` | MongoDB + Mongo Express | 27017 / 8081 | MongoDB 7.0 с веб-интерфейсом |
| `redis` | Redis + Redis Commander | 6379 / 8081 | Redis 7.2 с веб-интерфейсом |

### Брокеры сообщений

| Профиль | Сервисы | Порты | Описание |
|---------|---------|-------|----------|
| `kafka` | Kafka + UI + Schema Registry + Connect | 29092 / 8080 / 8081 / 8083 | Apache Kafka 8.0 (KRaft) с полным стеком |
| `rabbitmq` | RabbitMQ + Management UI | 5672 / 15672 | RabbitMQ 3.12 с веб-интерфейсом |

### Мониторинг и хранилища

| Профиль | Сервисы | Порты | Описание |
|---------|---------|-------|----------|
| `monitoring` | Prometheus + Grafana + Node Exporter | 9090 / 3000 / 9100 | Полный стек мониторинга |
| `minio` | MinIO + Console | 9000 / 9001 | S3-совместимое объектное хранилище |

## 💡 Примеры использования

### Пример 1: Запуск только баз данных

```bash
docker-compose --profile postgres --profile mongodb --profile redis up -d
```

### Пример 2: Запуск стека для микросервисов

```bash
docker-compose --profile postgres --profile redis --profile kafka --profile minio up -d
```

### Пример 3: Полный стек разработки

```bash
docker-compose --profile postgres --profile mongodb --profile redis \
               --profile kafka --profile rabbitmq --profile minio \
               --profile monitoring up -d
```

### Пример 4: Остановка конкретных сервисов

```bash
# Остановить только Kafka
docker-compose --profile kafka stop

# Остановить все запущенные сервисы
docker-compose stop
```

## ⚙️ Конфигурация

### Переменные окружения

Все настройки находятся в едином файле `.env`. Есть два способа настройки:

#### Способ 1: Автоматическая генерация секретов (рекомендуется)

**Общий скрипт (для всех сервисов):**
```bash
# Генерирует случайные пароли и обновляет все .env файлы
./generate-secrets.sh
```

**Отдельные скрипты (для конкретного сервиса):**
```bash
# Генерация секретов только для PostgreSQL
cd postgres && ./generate-secrets.sh

# Генерация секретов только для MongoDB
cd mongodb && ./generate-secrets.sh

# И так далее для каждого сервиса...
```

Общий скрипт автоматически:
- Генерирует безопасные случайные пароли для всех сервисов
- Обновляет корневой `.env` файл
- Обновляет `.env` файлы во всех подпапках (для обратной совместимости)
- Выводит все сгенерированные пароли для сохранения

Отдельные скрипты в подпапках:
- Генерируют секреты только для конкретного сервиса
- Удобны при работе с отдельными сервисами
- Работают независимо от общего скрипта

#### Способ 2: Ручная настройка

```bash
cp .env.example .env
# Отредактируйте .env файл и установите безопасные пароли
```

**Важно:** Все пароли по умолчанию — `admin` или `postgres`. Обязательно измените их в `.env` перед использованием!

### Инициализация баз данных

- **PostgreSQL**: SQL скрипты из `postgres/initdb-scripts/` автоматически выполняются при первом запуске
- **MongoDB**: JavaScript скрипты из `mongodb/initdb-scripts/` автоматически выполняются при первом запуске

### Сеть

Все сервисы используют единую Docker сеть `app-network`, поэтому они могут обращаться друг к другу по имени хоста (например, `postgres`, `redis`, `kafka`).

### Генерация секретов

В проекте есть два способа генерации секретов:

1. **Общий скрипт** (`./generate-secrets.sh`) — генерирует секреты для всех сервисов сразу
2. **Отдельные скрипты** в каждой подпапке — генерируют секреты только для конкретного сервиса

Пример использования отдельных скриптов:
```bash
# Только для PostgreSQL
cd postgres
./generate-secrets.sh

# Только для Redis
cd redis
./generate-secrets.sh
```

Это удобно, когда вы работаете с отдельными сервисами и не хотите обновлять все секреты сразу.

## 🔧 Полезные команды

### Управление сервисами

```bash
# Запуск сервисов с профилями
docker-compose --profile postgres --profile redis up -d

# Просмотр статуса запущенных сервисов
docker-compose ps

# Просмотр логов всех сервисов
docker-compose logs -f

# Просмотр логов конкретного сервиса
docker-compose logs -f postgres
docker-compose logs -f kafka

# Остановка всех сервисов
docker-compose stop

# Остановка и удаление контейнеров
docker-compose down

# Остановка с удалением volumes (⚠️ удалит данные!)
docker-compose down -v

# Перезапуск конкретного сервиса
docker-compose restart postgres

# Просмотр списка доступных сервисов
docker-compose config --services
```

### Документация по шаблонам

Каждый шаблон содержит подробную документацию с примерами использования:

- [PostgreSQL](postgres/README.md) — работа с PostgreSQL и pgAdmin
- [Kafka](kafka/README.md) — работа с Kafka, создание топиков, отправка сообщений
- [Redis](redis/README.md) — работа с Redis, примеры для Node.js, Python, Go
- [MongoDB](mongodb/README.md) — работа с MongoDB, примеры для разных языков
- [Prometheus + Grafana](prometheus-grafana/README.md) — настройка мониторинга, добавление метрик
- [RabbitMQ](rabbitmq/README.md) — работа с очередями, примеры для разных языков
- [MinIO](minio/README.md) — работа с S3-совместимым хранилищем

## 📝 Примечания

- Все сервисы настроены для **разработки** (не для продакшена)
- Kafka работает в режиме **KRaft** (без ZooKeeper) для упрощения
- Порты по умолчанию могут конфликтовать с другими сервисами — используйте переменные окружения в `.env` для изменения
- Данные сохраняются в Docker volumes и не удаляются при `docker-compose down` (используйте `-v` для удаления)
- Все сервисы находятся в единой сети `app-network` и могут обращаться друг к другу по имени хоста
- Используйте профили для запуска только нужных сервисов — это экономит ресурсы и ускоряет запуск

## 🔒 Безопасность

⚠️ **Важно для продакшена:**

- Измените все пароли по умолчанию
- Используйте секреты Docker или внешние системы управления секретами
- Настройте SSL/TLS для Kafka и PostgreSQL
- Ограничьте доступ к портам через firewall
- Используйте более строгие настройки репликации для Kafka

## 📄 Лицензия

Этот шаблон предоставляется "как есть" для использования в проектах разработки.
