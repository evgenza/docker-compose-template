# Docker Compose Template

Шаблон для быстрого запуска инфраструктурных сервисов с помощью Docker Compose. Включает готовые конфигурации для PostgreSQL и Apache Kafka с необходимыми инструментами управления.

## 📁 Структура проекта

```
docker-compose-template/
├── postgres/
│   ├── docker-compose.yaml      # Шаблон для PostgreSQL
│   ├── .env.example             # Пример переменных окружения
│   ├── README.md                # Документация PostgreSQL шаблона
│   └── initdb-scripts/          # SQL скрипты для инициализации БД
│       └── 01-init.sql
├── kafka/
│   ├── docker-compose.yaml      # Шаблон для Kafka
│   ├── .env.example             # Пример переменных окружения
│   └── README.md                # Документация Kafka шаблона
├── .gitignore
└── README.md
```

## 🚀 Быстрый старт

Каждый шаблон может использоваться независимо:

### PostgreSQL

```bash
cd postgres
cp .env.example .env
docker-compose up -d
```

Подробнее в [postgres/README.md](postgres/README.md)

### Kafka

```bash
cd kafka
cp .env.example .env
docker-compose up -d
```

Подробнее в [kafka/README.md](kafka/README.md)

## 📦 Включенные сервисы

### PostgreSQL Stack

- **PostgreSQL 15.8** (Alpine) — основная база данных
  - Порт: `5432` (настраивается через `POSTGRES_PORT`)
  - Данные сохраняются в volume `postgres_data`

- **pgAdmin 4** — веб-интерфейс для управления БД
  - Порт: `5050` (настраивается через `PGADMIN_PORT`)
  - URL: http://localhost:5050
  - Email/Password: настраиваются через переменные окружения

### Kafka Stack

- **Apache Kafka 8.0.0** (KRaft режим, без ZooKeeper)
  - Порт для клиентов: `29092` (настраивается через `KAFKA_PORT`)
  - JMX порт: `9101` (настраивается через `KAFKA_JMX_PORT`)
  - Данные сохраняются в volume `kafka_data`

- **Kafka UI** — веб-интерфейс для управления Kafka
  - Порт: `8080` (настраивается через `KAFKA_UI_PORT`)
  - URL: http://localhost:8080

- **Schema Registry** — управление схемами данных (Avro/JSON/Protobuf)
  - Порт: `8081` (настраивается через `SCHEMA_REGISTRY_PORT`)
  - URL: http://localhost:8081

- **Kafka Connect** — интеграция с внешними системами
  - Порт: `8083` (настраивается через `KAFKA_CONNECT_PORT`)
  - URL: http://localhost:8083

## ⚙️ Конфигурация

### Переменные окружения

Все настройки можно изменить через переменные окружения. Скопируйте `.env.example` в `.env` и отредактируйте значения:

```bash
# PostgreSQL
POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

# pgAdmin
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# Kafka
KAFKA_PORT=29092
KAFKA_JMX_PORT=9101
KAFKA_UI_PORT=8080
SCHEMA_REGISTRY_PORT=8081
KAFKA_CONNECT_PORT=8083
```

### Инициализация PostgreSQL

SQL скрипты из `postgres/initdb-scripts/` автоматически выполняются при первом запуске контейнера. Добавьте свои скрипты в эту папку с префиксом номера (например, `02-create-tables.sql`).

## 🔧 Полезные команды

### Управление сервисами

```bash
# Запуск в фоновом режиме
docker-compose up -d

# Остановка
docker-compose stop

# Остановка и удаление контейнеров
docker-compose down

# Остановка с удалением volumes (⚠️ удалит данные!)
docker-compose down -v

# Просмотр логов
docker-compose logs -f [service-name]

# Перезапуск конкретного сервиса
docker-compose restart [service-name]
```

### Работа с Kafka

```bash
# Создать топик
docker exec -it kafka-dev kafka-topics --create \
  --bootstrap-server localhost:29092 \
  --topic my-topic \
  --partitions 3 \
  --replication-factor 1

# Список топиков
docker exec -it kafka-dev kafka-topics --list \
  --bootstrap-server localhost:29092

# Отправить сообщение
docker exec -it kafka-dev kafka-console-producer \
  --bootstrap-server localhost:29092 \
  --topic my-topic

# Читать сообщения
docker exec -it kafka-dev kafka-console-consumer \
  --bootstrap-server localhost:29092 \
  --topic my-topic \
  --from-beginning
```

### Работа с PostgreSQL

```bash
# Подключение к БД
docker exec -it postgres-dev psql -U postgres -d postgres

# Выполнить SQL файл
docker exec -i postgres-dev psql -U postgres -d postgres < script.sql

# Бэкап БД
docker exec postgres-dev pg_dump -U postgres postgres > backup.sql

# Восстановление из бэкапа
docker exec -i postgres-dev psql -U postgres postgres < backup.sql
```

## 📝 Примечания

- Все сервисы настроены для **разработки** (не для продакшена)
- Kafka работает в режиме **KRaft** (без ZooKeeper) для упрощения
- Порты по умолчанию могут конфликтовать с другими сервисами — используйте переменные окружения для изменения
- Данные сохраняются в Docker volumes и не удаляются при `docker-compose down` (используйте `-v` для удаления)

## 🔒 Безопасность

⚠️ **Важно для продакшена:**

- Измените все пароли по умолчанию
- Используйте секреты Docker или внешние системы управления секретами
- Настройте SSL/TLS для Kafka и PostgreSQL
- Ограничьте доступ к портам через firewall
- Используйте более строгие настройки репликации для Kafka

## 📄 Лицензия

Этот шаблон предоставляется "как есть" для использования в проектах разработки.
